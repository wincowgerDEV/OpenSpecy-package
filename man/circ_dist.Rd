% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/masked_circular_ae_distances.R
\name{circ_dist}
\alias{circ_dist}
\alias{masked_reconstruction_loss}
\alias{masked_spectral_distance}
\title{Masked circular autoencoder distances and losses}
\usage{
circ_dist(theta1, theta2)

masked_reconstruction_loss(x, x_hat, mask)

masked_spectral_distance(
  x,
  mask = is.finite(x),
  method = c("correlation", "spectral_angle"),
  min_overlap_points = 100,
  min_overlap_fraction = 0.25,
  pairs = NULL,
  weights = c("overlap", "uniform")
)
}
\arguments{
\item{theta1, theta2}{Numeric angle vectors in degrees.}

\item{x}{Numeric matrix with spectra in rows and wavenumbers in columns.}

\item{x_hat}{Numeric matrix of reconstructed spectra matching \code{x}.}

\item{mask}{Logical matrix matching \code{x}; \code{TRUE} marks observed values.}

\item{method}{Target distance, either \code{"correlation"} or
\code{"spectral_angle"}.}

\item{min_overlap_points}{Minimum shared observed wavenumber count required
for a pairwise target distance.}

\item{min_overlap_fraction}{Minimum shared observed fraction required for a
pairwise target distance.}

\item{pairs}{Optional two-column matrix or data frame of one-based row pairs.}

\item{weights}{Pair weight strategy, either \code{"overlap"} or \code{"uniform"}.}
}
\value{
\code{circ_dist()} returns numeric circular distances scaled from 0 to 1.
\code{masked_reconstruction_loss()} returns one scalar mean loss.
\code{masked_spectral_distance()} returns a \code{data.table} with pairwise distances,
validity, overlap counts, overlap fractions, and weights.
}
\description{
Helper functions for the masked circular autoencoder workflow. These
functions do not use labels or metadata.
}
\examples{
circ_dist(0, 360)
circ_dist(0, 180)

x <- matrix(c(1, 2, NA, 1, 4, 5), nrow = 2, byrow = TRUE)
mask <- is.finite(x)
masked_spectral_distance(x, mask, min_overlap_points = 2)

}
\author{
Win Cowger
}
