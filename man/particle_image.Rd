% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/particle_image.R
\name{particle_image}
\alias{particle_image}
\title{Plot particle maps with base graphics}
\usage{
particle_image(
  x,
  material_col = "material_class",
  image = NULL,
  bottom_left = NULL,
  top_right = NULL,
  pixel_length = 1,
  origin = c(0, 0),
  palette = NULL,
  alpha = 0.8,
  labels = TRUE,
  label_col = "feature_id",
  main = "Particle Image",
  xlab = "X",
  ylab = "Y",
  legend = FALSE,
  pch = 15,
  cex = 1,
  ...
)
}
\arguments{
\item{x}{an \code{OpenSpecy} object, \code{Specs} object, or metadata table.}

\item{material_col}{column containing material or class labels.}

\item{image}{optional image path, array, raster, raw BMP bytes, or image
object. If \code{NULL}, an attached visual image is used when available.}

\item{bottom_left, top_right}{optional map extent in image pixel coordinates.}

\item{pixel_length}{map pixel length in plotting units when no image overlay
is used.}

\item{origin}{numeric length-2 x/y origin offset when no image overlay is
used.}

\item{palette}{named character vector mapping materials to colors.}

\item{alpha}{transparency for particle points.}

\item{labels}{logical; draw feature labels when feature IDs are present?}

\item{label_col}{column used for labels.}

\item{main, xlab, ylab}{plot labels.}

\item{legend}{logical; draw a base graphics legend?}

\item{pch, cex}{plotting character and size passed to
\code{\link[graphics]{points}()}.}

\item{\ldots}{additional arguments passed to \code{\link[graphics]{plot}()}.}
}
\value{
Invisibly returns the plotted metadata table.
}
\description{
\code{particle_image()} plots particle classifications from an \code{OpenSpecy},
\code{Specs}, or metadata table. When a visual image is attached with
\code{\link{add_visual_image}()} or supplied directly, particle coordinates
are transformed onto the image and drawn with transparency.
}
\examples{
tiny_map <- read_extdata("CA_tiny_map.zip") |> read_any()
tiny_map$metadata$material_class <- ifelse(tiny_map$metadata$x < 5,
                                           "poly(ethylene)", "mineral")
particle_image(tiny_map, legend = TRUE)

}
