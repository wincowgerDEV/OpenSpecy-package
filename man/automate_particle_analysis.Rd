% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/automate_particle_analysis.R
\name{automate_particle_analysis}
\alias{automate_particle_analysis}
\title{Automate particle analysis for spectral maps}
\usage{
automate_particle_analysis(
  x,
  library,
  output_dir = NULL,
  images = NULL,
  bottom_left = NULL,
  top_right = NULL,
  origins = NULL,
  material_col = "material_class",
  library_id_col = "sample_name",
  particle_id_strategy = c("collapse", "partial_collapse", "nonspatial_collapse",
    "all_cell_id", "raw"),
  spectral_smooth = FALSE,
  sigma1 = c(1, 1, 1),
  spatial_smooth = FALSE,
  sigma2 = c(3, 3),
  close = FALSE,
  close_kernel = c(4, 4),
  sn_threshold_min = 0.04,
  sn_threshold_max = Inf,
  cor_threshold = 0.7,
  area_threshold = 1,
  label_unknown = FALSE,
  remove_materials = NULL,
  remove_unknown = FALSE,
  pixel_length = 25,
  metric = "sig_times_noise",
  abs = FALSE,
  collapse_function = stats::median,
  outputs = c("details", "summary"),
  process_args = list(),
  specs_steps = c("pca", "kmeans"),
  specs_centers = NULL,
  ...
)
}
\arguments{
\item{x}{character vector of files, an \code{OpenSpecy}/\code{Specs} object, or a list
of objects/files.}

\item{library}{reference \code{OpenSpecy} object or trained model library passed
to \code{\link{match_spec}()}.}

\item{output_dir}{optional directory for CSV/RDS/PNG outputs.}

\item{images}{optional image path(s) or image objects aligned with \code{x}.}

\item{bottom_left, top_right}{optional lists of image corners; if missing and
an image is supplied, \code{\link{detect_image_origin}()} is attempted.}

\item{origins}{optional list with \code{x} and \code{y} origin offsets for map-unit
outputs.}

\item{material_col}{material/class column in matched library metadata.}

\item{library_id_col}{library metadata column used to join match metadata.}

\item{particle_id_strategy}{one of \code{"collapse"}, \code{"partial_collapse"},
\code{"nonspatial_collapse"}, \code{"all_cell_id"}, or \code{"raw"}.}

\item{spectral_smooth, sigma1}{apply 3D Gaussian smoothing to spectral maps;
file readers apply this while reading and in-memory maps are smoothed after
coercion.}

\item{spatial_smooth, sigma2}{passed to \code{\link{sig_noise}()} and
feature detection.}

\item{close, close_kernel}{passed to \code{\link{def_features}()}.}

\item{sn_threshold_min, sn_threshold_max}{signal/noise thresholds.}

\item{cor_threshold}{minimum match value for confident particle labels.}

\item{area_threshold}{minimum feature area in pixels.}

\item{label_unknown}{logical; label low-correlation matches as \code{"unknown"}.}

\item{remove_materials}{optional material labels to remove after matching.}

\item{remove_unknown}{logical; remove \code{"unknown"} after matching.}

\item{pixel_length}{map pixel length used for output dimensions.}

\item{metric, abs}{signal/noise arguments passed to \code{\link{sig_noise}()}.}

\item{collapse_function}{function used by \code{\link{collapse_spec}()}.}

\item{outputs}{character vector containing any of \code{"details"}, \code{"summary"},
\code{"particle_image"}, \code{"particle_heatmap"},
\code{"particle_heatmap_thresholded"}, \code{"cor_heatmap"}, \code{"raw"},
\code{"processed"}, or \code{"time"}. Short aliases \code{"heatmap"},
\code{"thresholded"}, and \code{"correlation"} are also accepted.}

\item{process_args}{optional named list overriding \code{\link{process_spec}()}
arguments for spectra before matching.}

\item{specs_steps, specs_centers}{compression controls for Specs-based
strategies.}

\item{\ldots}{catches removed legacy arguments and otherwise is reserved.}
}
\value{
A list with \code{samples}, \code{particle_details_all_csv}, and
\code{particle_summary_all_csv}. Each per-sample entry uses names matching file
exports where applicable: \code{particle_details_csv}, \code{particle_summary_csv},
\code{particles_raw_rds}, \code{particles_rds}, \code{particle_image_png},
\code{particle_heatmap_png}, \code{particle_heatmap_thresholded_jpg},
\code{cor_heatmap_png}, and \code{time_rds}. Image entries are recorded base-graphics
plots that can be replayed with \code{\link[grDevices]{replayPlot}()}.
}
\description{
\code{automate_particle_analysis()} generalizes the batch map workflow used for
particle detection, spectral matching, particle details, summaries, and
optional base-graphics particle images. Visual images attached to map objects
or read from supported H5 mosaics are used for particle color extraction when
feature definition is requested. It keeps file output optional and returns all
results as R objects.
}
\examples{
tiny_map <- read_extdata("CA_tiny_map.zip") |> read_any()
data("test_lib")
res <- automate_particle_analysis(tiny_map, test_lib,
                                  outputs = c("details", "summary"),
                                  sn_threshold_min = 0.1)
names(res)

}
