% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Specs.R, R/Specs_compact.R, R/Specs_file.R,
%   R/Specs_methods.R
\name{Specs}
\alias{Specs}
\alias{is_Specs}
\alias{check_Specs}
\alias{check_Specs.default}
\alias{check_Specs.Specs}
\alias{as_Specs}
\alias{as_Specs.default}
\alias{as_Specs.Specs}
\alias{as_Specs.OpenSpecy}
\alias{fit_specs_pca}
\alias{decompress_spec}
\alias{decompress_spec.default}
\alias{decompress_spec.Specs}
\alias{as_OpenSpecy.Specs}
\alias{encode_specs_hilbert}
\alias{decode_specs_hilbert}
\alias{write_specs}
\alias{write_specs.default}
\alias{write_specs.Specs}
\alias{read_specs}
\alias{specs_background_filter}
\alias{specs_source_count}
\alias{specs_background_mask}
\alias{specs_source_values}
\alias{specs_coordinates}
\alias{specs_metadata}
\alias{write_specs.FileSpecs}
\alias{cor_spec.Specs}
\alias{match_spec.Specs}
\alias{def_features.Specs}
\alias{collapse_spec.Specs}
\title{Create compressed Specs objects}
\usage{
Specs(variables, values, coords = NULL, metadata = NULL, attributes = list())

is_Specs(x)

check_Specs(x, ...)

\method{check_Specs}{default}(x, ...)

\method{check_Specs}{Specs}(x, ...)

as_Specs(x, ...)

\method{as_Specs}{default}(x, ...)

\method{as_Specs}{Specs}(
  x,
  model = NULL,
  steps = NULL,
  background_filter = NULL,
  n_components = NULL,
  centers = NULL,
  bits_per_variable = NULL,
  limits = NULL,
  ...
)

\method{as_Specs}{OpenSpecy}(
  x,
  model = NULL,
  steps = c("pca", "hilbert"),
  background_filter = NULL,
  n_components = NULL,
  centers = NULL,
  bits_per_variable = NULL,
  limits = NULL,
  ...
)

fit_specs_pca(x, n_components, center = TRUE, scale. = FALSE, ...)

decompress_spec(x, ...)

\method{decompress_spec}{default}(x, ...)

\method{decompress_spec}{Specs}(x, expand = TRUE, index = NULL, ...)

\method{as_OpenSpecy}{Specs}(x, ...)

encode_specs_hilbert(x, bits_per_variable = NULL, limits = NULL, ...)

decode_specs_hilbert(x, ...)

write_specs(x, file, compress = "xz", ...)

\method{write_specs}{default}(x, file, compress = "xz", ...)

\method{write_specs}{Specs}(x, file, compress = "xz", ...)

read_specs(file, ...)

specs_background_filter(
  metric = "run_sig_over_noise",
  minimum,
  maximum = Inf,
  sigma = NULL,
  step = 10
)

specs_source_count(x)

specs_background_mask(x, index = NULL)

specs_source_values(x, index = NULL)

specs_coordinates(x, index = NULL, columns = NULL)

specs_metadata(x, index = NULL, columns = NULL)

\method{write_specs}{FileSpecs}(x, file, compress = "xz", ...)

\method{cor_spec}{Specs}(x, library, na.rm = TRUE, compute = "optimized", ...)

\method{match_spec}{Specs}(
  x,
  library,
  top_n = NULL,
  expand = FALSE,
  add_library_metadata = NULL,
  add_object_metadata = NULL,
  compute = "optimized",
  na.rm = TRUE,
  ...
)

\method{def_features}{Specs}(
  x,
  features,
  shape_kernel = c(3, 3),
  shape_type = "box",
  close = FALSE,
  close_kernel = c(4, 4),
  close_type = "box",
  img = NULL,
  bottom_left = NULL,
  top_right = NULL,
  ...
)

\method{collapse_spec}{Specs}(x, fun = mean, column = "feature_id", ...)
}
\arguments{
\item{variables}{vector of latent variable names.}

\item{values}{numeric matrix with one row per variable and one column per
active spectrum or cluster.}

\item{coords}{coordinate \code{data.frame} or \code{data.table}; should
include \code{x}, \code{y}, \code{source_id}, and \code{value_id}; or an
internal validated compact regular-grid descriptor returned by map readers.}

\item{metadata}{metadata \code{data.frame} or \code{data.table} with one row
per column in \code{values}.}

\item{attributes}{list of Specs attributes to attach.}

\item{x}{an object to test, convert, decompress, or write.}

\item{model}{optional \code{SpecsPCA} model returned by
\code{fit_specs_pca()}; if omitted and \code{"pca"} is in \code{steps}, a
model is fit from \code{x}.}

\item{steps}{character vector of compression steps. Supported values are
\code{"background"}, \code{"pca"}, \code{"kmeans"}, and \code{"hilbert"}.
Background suppression must precede every compression step. K-means can be
placed before, between, or after the other compression steps; PCA cannot be
placed after Hilbert encoding.}

\item{background_filter}{optional policy returned by
\code{specs_background_filter()}. Suppression is explicit and lossy and
records the source mask, signal/noise values, reasons, and policy.}

\item{n_components}{number of PCA components to keep.}

\item{centers}{initial centers or the number of centers for weighted Lloyd
K-means. Source mapping multiplicities supply the weights and mapping 0 is
excluded.}

\item{bits_per_variable}{positive whole number of bits used for each
Hilbert-encoded variable. If \code{NULL}, the value is inferred from the
number of variables to pack the available 64-bit code space.}

\item{limits}{optional two-column matrix, data frame, or Hilbert model with
per-variable minimum and maximum values used for quantization.}

\item{center, scale.}{arguments passed to \code{\link[stats]{prcomp}()}.}

\item{expand}{logical; if \code{TRUE}, decompress or match one row per
original coordinate; if \code{FALSE}, keep active spectra or clusters.}

\item{index}{optional positive integer vector selecting spectra to
decompress. With \code{expand = TRUE}, indexes refer to rows in
\code{x$coords}; with \code{expand = FALSE}, indexes refer to columns in
\code{x$values}.}

\item{file}{file path for reading or writing a Specs object.}

\item{compress}{compression argument passed to \code{\link[base]{saveRDS}()}.}

\item{metric}{signal/noise metric passed to \code{\link[=sig_noise]{sig_noise()}}.}

\item{minimum, maximum}{strict accepted signal/noise bounds.}

\item{sigma}{optional three-dimensional Gaussian smoothing sigma. \code{NULL}
classifies the unsmoothed spectra.}

\item{step}{run-length step passed to \code{\link[=sig_noise]{sig_noise()}}.}

\item{columns}{optional coordinate or metadata columns to return.}

\item{library}{a \code{Specs} object to match against.}

\item{na.rm}{logical; should missing values be removed for latent matching?}

\item{compute}{correlation compute strategy, \code{"optimized"} or
\code{"base"}.}

\item{top_n}{integer; number of top latent matches to return.}

\item{add_library_metadata}{name of a library metadata column to join.}

\item{add_object_metadata}{name of an object metadata column to join.}

\item{features}{logical or character vector with one value per row in
\code{x$coords}.}

\item{shape_kernel, shape_type, close, close_kernel, close_type, img, bottom_left, top_right}{arguments passed to the feature-definition routine.}

\item{fun}{function used to collapse latent values.}

\item{column}{coordinate column used to group spectra for collapse.}

\item{\ldots}{additional arguments passed to submethods.}
}
\value{
\code{Specs()}, \code{as_Specs()}, \code{encode_specs_hilbert()}, and
\code{decode_specs_hilbert()} return a \code{Specs} object.
\code{fit_specs_pca()} returns a \code{SpecsPCA} model.
\code{decompress_spec()} returns an exact \code{OpenSpecy} object for
uncompressed values, an approximate reconstruction after PCA/Hilbert, and an
exact zero line for every background-suppressed source.
\code{read_specs()} returns a \code{Specs} object.
}
\description{
\code{Specs} objects store compressed spectral data for large hyperspectral
datasets. They use a structure similar to \code{OpenSpecy}, but store
physical or latent \code{variables}, active \code{values}, coordinate data,
and metadata separately. Version 0.2 objects may represent regular grids and
repeated metadata compactly and reserve source mapping 0 for explicitly
background-suppressed pixels.
}
\examples{
data("raman_hdpe")
specs <- as_Specs(raman_hdpe, n_components = 1)
decompress_spec(specs)

}
\author{
Win Cowger
}
