% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_h5.R
\name{split_h5}
\alias{split_h5}
\title{Split a large HDF5 spectral map by metadata category}
\usage{
split_h5(
  file,
  field = "region",
  output_dir = dirname(file),
  format = c("h5", "rds")
)
}
\arguments{
\item{file}{path to one \code{.h5} or \code{.hdf5} file.}

\item{field}{metadata field used to group spectra. Defaults to \code{"region"}.}

\item{output_dir}{directory in which to save the output files. Defaults to the
source file's directory.}

\item{format}{output format. \code{"h5"} (the default) uses native HDF5 object
copies for whole-region categories. \code{"rds"} materializes one category as
an \code{OpenSpecy} object before saving it.}
}
\value{
Invisibly, a named character vector of output paths. Names are the
original field-category labels. H5 outputs remain readable by \code{\link[=read_h5]{read_h5()}}
and \code{\link[=open_specs]{open_specs()}}; each RDS file contains one \code{OpenSpecy} object.
}
\description{
\code{split_h5()} groups an H5 or HDF5 spectral map by a metadata field. The
default region-to-H5 path discovers region groups directly and copies them
without hashing the complete source, building a per-pixel index, or loading
spectral values into R. Other metadata fields use a file-backed
\link[=open_specs]{FileSpecs} descriptor. \code{format = "rds"} reads and saves one
category at a time; each RDS category must fit in memory.
}
\details{
Output files are named \verb{<input stem>_<field category>.<format>}. Characters
that are not valid in Windows file names are replaced with underscores.
Existing output files are never overwritten. Native H5 output requires each
category to contain complete source regions because the source schema stores
spectra as three-dimensional regional grids. Use \code{format = "rds"} for a
field that divides pixels within a region. File-level metadata and selected
region groups are retained in H5 outputs. When registered mosaic metadata is
available, each H5 output also retains the intersecting image tiles and their
corresponding centers so visual-image registration remains self-contained.
}
\examples{
\dontrun{
split_h5("large_map.h5")
split_h5("large_map.h5", field = "particle_id", output_dir = "regions",
         format = "rds")
}

}
\seealso{
\code{\link[=open_specs]{open_specs()}}, \code{\link[=decompress_spec]{decompress_spec()}}
}
