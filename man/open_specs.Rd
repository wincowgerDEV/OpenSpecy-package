% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Specs_file.R, R/Specs_methods.R
\name{open_specs}
\alias{open_specs}
\alias{as_Specs.FileSpecs}
\alias{print.FileSpecs}
\alias{check_Specs.FileSpecs}
\alias{decompress_spec.FileSpecs}
\alias{split_spec.FileSpecs}
\alias{cor_spec.FileSpecs}
\alias{match_spec.FileSpecs}
\alias{def_features.FileSpecs}
\alias{collapse_spec.FileSpecs}
\title{Open a large spectral source as file-backed Specs}
\usage{
open_specs(path, cache_dir = NULL)

\method{as_Specs}{FileSpecs}(x, ...)

\method{print}{FileSpecs}(x, ...)

\method{check_Specs}{FileSpecs}(x, ...)

\method{decompress_spec}{FileSpecs}(x, index = NULL, region = NULL, roi = NULL, bands = NULL, ...)

\method{split_spec}{FileSpecs}(x, by = "region", ...)

\method{cor_spec}{FileSpecs}(x, ...)

\method{match_spec}{FileSpecs}(x, ...)

\method{def_features}{FileSpecs}(x, ...)

\method{collapse_spec}{FileSpecs}(x, ...)
}
\arguments{
\item{path}{path to an H5 file, an ENVI binary file, or its \code{.hdr} file.}

\item{cache_dir}{directory for immutable derived cache generations. The
default uses the user cache directory, never the source directory.}

\item{x}{a \code{FileSpecs} object.}

\item{...}{additional arguments reserved for methods.}

\item{index}{positive row positions in the current file-backed view.}

\item{region}{optional region names to materialize.}

\item{roi}{optional numeric \code{c(xmin, xmax, ymin, ymax)} selection or a list
with two-element \code{x} and \code{y} ranges.}

\item{bands}{optional positive spectral-band positions to materialize.}

\item{by}{grouping field; file-backed splitting currently supports only
\code{"region"}.}
}
\value{
\code{open_specs()} returns a descriptor-only \code{FileSpecs} object.
\code{decompress_spec()} returns a bounded \code{OpenSpecy} materialization and
\code{split_spec()} returns lightweight \code{FileSpecs} views.
}
\description{
\code{FileSpecs} keeps a durable, read-only description of a large H5 or ENVI
source. Spectral values are read only when an explicit bounded selection is
materialized. The object never stores an open connection or modifies a
source member.
}
