% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_spec.R
\name{assess_spec}
\alias{assess_spec}
\alias{assess_spec.default}
\alias{assess_spec.OpenSpecy}
\title{Assess common spectral quality issues}
\usage{
assess_spec(x, ...)

\method{assess_spec}{default}(x, ...)

\method{assess_spec}{OpenSpecy}(
  x,
  checks = c("high_tail", "silent_region", "co2_region", "missing_values",
    "flat_spectrum", "negative_intensity", "low_snr"),
  high_prob = 0.9,
  artifact_ratio = 3,
  tail_n = 5L,
  silent_region = c(1800, 2000),
  co2_region = c(2200, 2420),
  snr_threshold = 4,
  flat_tol = sqrt(.Machine$double.eps),
  negative_tol = 0,
  na.rm = TRUE,
  ...
)
}
\arguments{
\item{x}{an \code{OpenSpecy} object.}

\item{checks}{character; checks to run. Options include
\code{"high_tail"}, \code{"silent_region"}, \code{"co2_region"},
\code{"missing_values"}, \code{"flat_spectrum"},
\code{"negative_intensity"}, and \code{"low_snr"}.}

\item{high_prob}{numeric; spectrum-wide quantile used as the high intensity
threshold for the silent-region check.}

\item{artifact_ratio}{numeric; minimum ratio between the normalized maximum
in a tail or carbon dioxide region and the normalized maximum outside both
artifact regions required to flag an issue.}

\item{tail_n}{integer; number of points to check at each end of the spectrum.}

\item{silent_region}{numeric length two; wavenumber range expected to be
mostly silent.}

\item{co2_region}{numeric length two; carbon dioxide wavenumber range.}

\item{snr_threshold}{numeric; spectra with run signal-to-noise below this
value are flagged.}

\item{flat_tol}{numeric; maximum finite intensity range considered flat.}

\item{negative_tol}{numeric; minimum allowed intensity before a spectrum is
flagged as negative.}

\item{na.rm}{logical; indicating whether missing values should be removed
when calculating thresholds and metrics.}

\item{\ldots}{further arguments passed to \code{\link{sig_noise}()} for the
\code{"low_snr"} check.}
}
\value{
A \code{\link[data.table]{data.table-class}()} with one row per issue found
and columns describing the spectrum, check, issue, likely cause, potential
fix, metric value, threshold, and region. If no issues are found, an empty
\code{data.table} with the same columns is returned.
}
\description{
\code{assess_spec()} scans spectra for common quality-control issues and
returns one row for each issue found.
}
\examples{
data("raman_hdpe")
assess_spec(raman_hdpe)

}
\author{
Win Cowger
}
