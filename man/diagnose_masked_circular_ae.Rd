% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/masked_circular_ae_diagnostics.R
\name{diagnose_masked_circular_ae}
\alias{diagnose_masked_circular_ae}
\alias{plot_circular_embedding}
\title{Diagnostics and plotting for masked circular autoencoders}
\usage{
diagnose_masked_circular_ae(
  model,
  x,
  wavenumber = NULL,
  encoded = NULL,
  target_distance = model$target_distance,
  n_pairs = 10000,
  k = 5,
  metadata = NULL,
  seed = 1
)

plot_circular_embedding(
  encoded,
  color_by = NULL,
  label_by = NULL,
  metadata = NULL
)
}
\arguments{
\item{model}{A \code{MaskedCircularAEModel}.}

\item{x}{Spectra compatible with \code{model}.}

\item{wavenumber}{Optional wavenumber grid for matrix-like \code{x}.}

\item{encoded}{Optional encoded coordinates from
\code{encode_masked_circular_ae()}.}

\item{target_distance}{Target spectral distance for diagnostic comparison.}

\item{n_pairs}{Maximum number of sampled spectrum pairs.}

\item{k}{Number of neighbors for neighbor-preservation diagnostics.}

\item{metadata}{Optional metadata used only after training.}

\item{seed}{Random seed for sampled pair diagnostics.}

\item{color_by}{Optional metadata or encoded column used for point color.}

\item{label_by}{Optional metadata or encoded column used for point labels.}
}
\value{
\code{diagnose_masked_circular_ae()} returns a list of diagnostic tables and
summaries. \code{plot_circular_embedding()} draws a base R circular plot and
invisibly returns the plotting data.
}
\description{
Post-training diagnostics for a fitted masked circular autoencoder. Metadata
may be used here for display or summary after training, but never updates the
model or loss history.
}
\examples{
\dontrun{
data("test_lib")
model <- fit_masked_circular_ae(test_lib, decoder_degree = 2)
encoded <- encode_masked_circular_ae(model, test_lib)
diagnose_masked_circular_ae(model, test_lib, encoded = encoded)
plot_circular_embedding(encoded)
}

}
\author{
Win Cowger
}
