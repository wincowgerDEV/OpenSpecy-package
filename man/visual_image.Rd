% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visual_image.R
\name{add_visual_image}
\alias{add_visual_image}
\alias{visual_image}
\alias{detect_image_origin}
\title{Attach visual images to spectral map objects}
\usage{
add_visual_image(
  x,
  image,
  bottom_left = NULL,
  top_right = NULL,
  source = NULL,
  detection_method = NULL,
  diagnostics = NULL,
  transform = NULL,
  ...
)

visual_image(x, require = FALSE, ...)

detect_image_origin(
  image,
  red_threshold = 50,
  red_ratio = 2,
  min_pixels = 1,
  diagnostics = TRUE,
  ...
)
}
\arguments{
\item{x}{an \code{OpenSpecy} or \code{Specs} object.}

\item{image}{image path, raster, matrix, array, raw BMP bytes, or an existing
visual-image list.}

\item{bottom_left, top_right}{numeric length-2 vectors giving the lower-left
and upper-right corners of the spectral map in image pixel coordinates.
Image x values are left-to-right and image y values are top-to-bottom.}

\item{source}{optional image source label or file path.}

\item{detection_method}{optional description of how the origin was detected.}

\item{diagnostics}{optional diagnostic data to store with the image.}

\item{transform}{optional list describing a custom transform.}

\item{require}{logical; should \code{visual_image()} error when no image is
attached?}

\item{red_threshold}{minimum red-channel value on the 0-255 scale.}

\item{red_ratio}{minimum red-to-green and red-to-blue ratio for red-box
detection.}

\item{min_pixels}{minimum number of red pixels required for origin detection.}

\item{\ldots}{reserved for future methods.}
}
\value{
\code{add_visual_image()} returns \code{x} with a \code{visual_image} attribute.
\code{visual_image()} and \code{detect_image_origin()} return lists.
}
\description{
\code{add_visual_image()} stores a visual image and map-to-image alignment metadata
on an \code{OpenSpecy} or \code{Specs} object. \code{visual_image()} retrieves that
attribute. \code{detect_image_origin()} detects red Thermo Fisher iN10 map boxes
in visual images and returns the image coordinates needed for overlays and
feature color extraction.
}
\examples{
data("raman_hdpe")
img <- array(1, dim = c(10, 10, 3))
with_image <- add_visual_image(raman_hdpe, img,
                               bottom_left = c(1, 10),
                               top_right = c(10, 1))
visual_image(with_image)$bottom_left

}
