% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_lib.R
\name{predict_class_reference}
\alias{predict_class_reference}
\title{Predict blank class-reference values with reviewed regex rules}
\usage{
predict_class_reference(
  metadata,
  regex_reference,
  return = c("table", "report")
)
}
\arguments{
\item{metadata}{A data.frame or data.table with \code{spectrum_identity} and
\code{material} columns. Row order is preserved.}

\item{regex_reference}{A data.frame or data.table with unique \code{pattern} and
nonblank \code{material} columns.}

\item{return}{Return the updated table or an audit containing \code{data},
\code{summary}, \code{predictions}, \code{clashes}, and \code{overlaps}.}
}
\value{
An updated data.table, or an audit list when \code{return = "report"}.
}
\description{
Applies a separate regex reference only to rows whose \code{material} is blank.
Populated exact materials are authoritative and are never overwritten, even
when a regex also matches them. A blank row is filled only when all matching
patterns name one material. Distinct-material matches remain blank and are
reported as clashes. Exact/regex overlaps are allowed and reported for QA.
If present, \code{match_identity} is used for pattern matching while
\code{spectrum_identity} remains the reported exact identity.
}
