% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_metrics.R
\name{crowd_lookup}
\alias{crowd_lookup}
\alias{recovery_rate}
\alias{minimum_detectable_amount}
\alias{batch_detection_limit}
\title{Particle analysis validation metrics}
\usage{
crowd_lookup(
  x,
  sample_col = "sample_id",
  area_col = "area_um2",
  size_col = "min_length_um",
  material_col = NULL,
  group_cols = sample_col,
  size_threshold = 500,
  surface_area = NULL,
  simulations = 10000,
  seed = NULL,
  na.rm = TRUE
)

recovery_rate(
  x,
  observed_col = "count",
  expected_col = "total_spiked",
  group_cols = NULL,
  pre_recovered_col = NULL,
  na.rm = TRUE
)

minimum_detectable_amount(
  x,
  count_col = "count",
  group_cols = NULL,
  offset = 3,
  md_multiplier = 3.29,
  bdl_multiplier = 4.65,
  spike_replicates = 4,
  round = c("integer", "ceiling", "none"),
  na.rm = TRUE
)

batch_detection_limit(
  x,
  count_col = NULL,
  offset = 3,
  multiplier = 4.65,
  round = c("integer", "ceiling", "none"),
  ...
)
}
\arguments{
\item{x}{a data frame or data table.}

\item{sample_col}{column identifying samples.}

\item{area_col}{column containing particle area.}

\item{size_col}{optional column containing particle size. If \code{NULL}, size is
inferred as \code{sqrt(area_col)}.}

\item{material_col}{optional material column to include in crowding groups.}

\item{group_cols}{columns used for grouped summaries.}

\item{size_threshold}{particles larger than this size are assessed for
possible crowding.}

\item{surface_area}{optional analyzed surface area used to calculate percent
area covered.}

\item{simulations}{number of simulated small-particle cumulative-area draws.}

\item{seed}{optional random seed for reproducible crowding simulations.}

\item{na.rm}{logical; remove missing values from summaries?}

\item{observed_col, expected_col}{columns with observed and expected spike
counts.}

\item{pre_recovered_col}{optional column with particles recovered before the
automated analysis.}

\item{count_col}{column with blank counts.}

\item{offset, md_multiplier, bdl_multiplier}{numeric constants used in MDA and
BDL formulas.}

\item{spike_replicates}{number of spike replicates in the MDA formula.}

\item{round}{one of \code{"integer"}, \code{"ceiling"}, or \code{"none"} for detection-limit
rounding. \code{"integer"} matches the historic workflow's \code{as.integer()}.}
}
\value{
A \code{data.table} containing the requested summary.
}
\description{
Helpers for assessing particle crowding, spike recovery, minimum detectable
amount (MDA), and batch detection limit (BDL) from particle-count tables.
}
\examples{
blanks <- data.frame(sample_id = c("b1", "b2", "b3", "b4"),
                     area_bins = "(0,212]",
                     count = c(0, 0, 0, 11))
minimum_detectable_amount(blanks, group_cols = "area_bins")
batch_detection_limit(11)

}
